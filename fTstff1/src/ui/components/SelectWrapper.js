import styled from 'styled-components';

export default styled.div`
  .select__control {
    box-shadow: none;
    min-width: 195px;
    border-color: rgba(0, 0, 0, 0.2);

    :hover {
      border-color: #101010;
    }
  }
`;
