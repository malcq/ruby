import { Component, Input, Output, EventEmitter, } from '@angular/core';

@Component({
  selector: 'app-menu-item-categories',
  template: ''
})
export class MockMenuItemCategoriesComponent {
  @Input() title = '';
}
