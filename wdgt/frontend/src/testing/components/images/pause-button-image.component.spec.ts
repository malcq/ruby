import { Component, Input, EventEmitter, } from '@angular/core';

@Component({
  selector: 'app-pause-button-image',
  template: ''
})
export class MockPauseButtonImageComponent {
  @Input() size = 50;
}
